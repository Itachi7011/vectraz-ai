# Used by Prisma CLI commands run from packages/db (generate, push, migrate,
# seed, studio). Must match the DATABASE_URL in both microservices' .env
# files — all three point at the same Postgres database/schema.
DATABASE_URL="postgresql://vectrazai:vectrazai_dev_password@localhost:5432/vectrazai?schema=public"
